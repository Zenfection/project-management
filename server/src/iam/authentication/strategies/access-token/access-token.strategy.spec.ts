import { AccessTokenStrategy } from './access-token.strategy';

describe('AccessTokenStrategy', () => {
  it('should be defined', () => {
    expect(new AccessTokenStrategy()).toBeDefined();
  });
});
